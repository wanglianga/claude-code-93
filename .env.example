# 宿主机映射端口（仅发布 web 门户；PostgreSQL 与后端均不发布到宿主）
CC_PUBLISH_PORT=3193

# 可选：覆盖 compose 项目名（验证环境通常已由 COMPOSE_PROJECT_NAME 固定）
# COMPOSE_PROJECT_NAME=task2026091793b

# JWT 密钥（生产环境请更换）
JWT_SECRET=ccb-branch-queue-demo-secret-key-please-change-in-prod-0123456789
