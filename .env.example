# 宿主机映射端口（由验证环境注入，本地可自行修改，避免与并行任务冲突）
CC_PUBLISH_PORT=3093
# compose 项目名（验证环境固定，勿改）
COMPOSE_PROJECT_NAME=task2026091793a

# PostgreSQL 配置（仅容器内网访问，不发布到宿主）
POSTGRES_DB=ccb_queue
POSTGRES_USER=ccb
POSTGRES_PASSWORD=ccb_pass_2026
